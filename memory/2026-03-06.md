# 2026-03-06 — PolyClawster Session

## What happened
- Synced workspace with `app/main` (polyclawster-app private repo)
- Cleaned Supabase: removed 13 test users, kept 5 real ones (alienjesus, virixlabs, Ellija, akvise, Ballista_premium)
- Removed compromised wallet `0x3eAe9f8a` refs + seed phrases from entire codebase
- New wallet: `0x6f314d7d` (unfunded)
- Rewrote `/api/signals.js` (Gamma API) and `/api/portfolio.js` (no more stale data.json)
- Redesigned Portfolio + Wallet tabs in TMA (Russian locale, minimalist)
- Built Demo/Live toggle slider with new IDs (`wallet-lbl-demo`, `wallet-lbl-live`, `wallet-toggle-thumb`, `wallet-toggle-track`)
- Deployed to Vercel successfully

## Still TODO
- **Toggle markup mismatch**: `walletDataHTML()` still uses old IDs (`wallet-mode-label`, `wallet-mode-toggle`) — needs updating to match `switchWalletMode()`
- **`buildWalletBalance()` Russian strings** need fixing
- **Supabase key rotation** — service role key was in public repo (user needs to be at computer)
- **Encrypt `wallets.private_key_enc`** — still plaintext in Supabase
- **Fund wallet `0x6f314d7d`** with USDC.e for auto-trading

## Key refs
- Vercel project: `prj_HNaEDtZSZaBdLCZVl4U4PpF9oEfy`, team `team_IqbE6zG7YNPJNSDm41DhihuO`
- Git: `origin` = skill repo, `app` = product repo, branch `main`
- TMA: single file `tma/src/index.html` (~5000 lines)
- Deploy: Vercel API POST with `gitSource.repoId: "1171890089"`

## UI State Snapshot (as of 14:30 UTC)

### Navigation (bottom bar, 4 tabs)
Port | Live | Agents | Wallet
- Stats tab removed
- Chat tab hidden (display:none)

### Screen Layout (no headers on main tabs)
All main tabs: no header, content starts with safe-area padding:
`padding-top: calc(8px + var(--tg-content-safe-area-inset-top, env(safe-area-inset-top, 0px)))`

Sub-screens (signal detail, position detail, wallet detail): keep header with Back button, transparent bg.

### Portfolio Tab (s-portfolio)
1. Live ticker (horizontal scrolling whale chips) — transparent background
2. Hero balance: DEMO БАЛАНС / $X.XX (total = free + inBets)
3. Two buttons: ⚡ Сигналы | 💰 Пополнить
4. Demo balance info card
5. ТОП РЫНКИ section (signals with icons from Polymarket)
6. Ставки section (active bets with market icons, side badge, PnL, end date)

### Live Tab (s-signals)
Sub-tabs: Signals | Whales | Movers
- All three have unified card style: [44px icon] | title (2-line clamp) + meta | value + YES/NO buttons
- Signals: icon from Polymarket, score color-coded, YES/NO prices
- Whales: icon from Polymarket (when available), whale emoji, amount, side+price, time ago
- Movers: icon from Polymarket, YES/NO prices, volume, YES price big

### Wallet Tab (s-wallet)
1. Demo/Live toggle: two labels (Демо/Live) flanking slider
   - IDs: wallet-lbl-demo, wallet-lbl-live, wallet-toggle-thumb, wallet-toggle-track
   - Track onclick: `switchWalletMode(!window._walletIsDemo)`
   - Thumb: pointer-events:none
   - Purple for demo, green for live
2. Balance display (buildWalletBalance)
3. Пополнить / Вывести buttons
4. Collapsible: deposit address, referral program
5. Transaction history

### Bet Sheet (bottom sheet overlay)
- Market header: 48px image + title + type label (🐋 Кит / 📈 Высокий объём / ⚡ Сигнал)
- Amount input: $ prefix, default $1, inline
- Preset buttons row: $1 | $5 | $10 | $25 (equal width, below input)
- Payout line: YES → $X · NO → $X
- Balance hint (right-aligned): демо $X.XX or $X.XX
- Two big buttons at bottom: YES ↑ (green) + NO ↓ (red) with price in cents
- Clicking YES/NO = setBetSide + confirmBetSheet (instant)

### Bet Detail (overlay on bet card click)
- Fetches /api/market-info for live CLOB prices
- Market image + title + side badge + ДЕМО tag + status
- Two hero boxes: current price (cents) + PnL ($, %)
- Stats table: amount, buy price, payout if win, market close date (days left), liquidity, volume 24h, AI score, created date
- Buttons: 🔻 Продать | 🔺 Докупить
- Polymarket link

### Global State Sync
- refreshAll(delayMs) — called after bet/sell/deposit
- Clears _portfolio, _mktInfoCache, reloads loadPortfolio + loadWallet + hero balance

### Safe Area
- Only `--tg-content-safe-area-inset-top` (not stacked with device safe area)
- 8px static padding
- Telegram header color: #0d0820 (matches --bg)

### Key Colors
- --bg: #0d0820
- --accent: #9945FF
- Demo: purple #a78bfa / rgba(139,92,246,*)
- Live/YES: green #19fb9b
- NO: red #FF4F4F
- Score high (8+): #19fb9b, med (6+): #F7B731, low: #45AAF2

### API Endpoints
- /api/signals — Gamma API top markets with icons
- /api/feed — whale trades + movers (enriched with Gamma images)
- /api/market-info?ids=... — CLOB prices (authoritative) + Gamma metadata (only if matching)
- /api/sell — close demo bets (status → cancelled), return PnL to demo_balance
- /api/trade — place bets (demo deducts demo_balance, real via CLOB)
- /api/portfolio — user portfolio data
- /api/demo-bonus — $1 demo grant (checks demo_balance > 0 OR total_deposited > 0)
