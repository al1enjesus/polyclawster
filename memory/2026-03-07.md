
---
## Session: 2026-03-07 (pre-compaction flush)

### Project: PolyClawster TMA Redesign

#### Completed in this session
- Skeleton loading, pull-to-refresh, galactic animated background
- Bot deeplink buttons (`TMA_LINK()` → `https://t.me/PolyClawsterBot/app?startapp=SOURCE`)
- CORS fix — all gamma/CLOB calls proxied via `/api/price-history.js`
- Onboarding: 6 screens, OB_KEY=`pc_ob_v6_TGID` (per-user), server-side `onboarded` field in Supabase
- `/api/set-onboarded.js` endpoint registered in vercel.json
- Bet confirmation modal redesign (✅ icon, share buttons, "Что дальше")
- High-converting paywall (`showNoBetSheet`) with multiplier display
- Fixed demo bet price (was saving price:0; now sends `priceYes`/`priceNo` from signal)
- Fixed P&L formula: `shares=amt/buyPrice; value=shares*curPrice; pnl=value-amt`
- Fixed sellBet() same shares model; buyPrice fallback = curPrice (not 0 or 0.5)
- QR code loader with spinner
- Chart intervals 15M/1H/1W/1M/ALL; default 1H; slices 1d data
- Referral notification with @username + Russian text
- Signal detail redesign: event image card + chart + AI reasoning + sticky YES/NO buttons
- Syntax fixes: removed unused `betSection`, fixed optional chaining

#### In Progress / Pending
- **Chart interval bug**: `switchChartInterval()` checks `detail-price-chart` and `bet-detail-chart` but NOT `sd-price-chart` (signal detail) → intervals don't switch on signal detail
- **Amount input on signal detail**: Need quick-select ($1/$5/$10/$25) + text input above sticky YES/NO CTAs in `#sd-sticky-cta`
- **NOWPayments integration**: Waiting for API key from user; UI placeholder ready
- **Position detail redesign** (`renderPositionDetail`): simplified 3-number layout (Вложил / Стоит сейчас / Если сбудется)
- **Bot restart needed**: Any bot-v2.js changes require `pm2 restart polyclawster-bot` on PM2 server

#### Critical File Locations
- TMA: `/workspace/tma/src/index.html` (~5000+ lines)
- `switchChartInterval()` ~line 3327
- `renderSignalDetail(s)` ~line 3801
- `renderPositionDetail(p)` ~line 3349
- `openBetDetail(betId)` ~line 5640
- Latest commit: `56c6e4e` ("feat: redesign signal detail — event card + chart + AI analysis + sticky buy buttons")

#### Key Technical Decisions
- Stars → `demo_balance` (correct by design, not real USDC)
- `buyPrice fallback = curPrice` when `bet.price = 0` (old bets show 0% not -87%)
- Chart globals: `window._chartTokenId`, `window._chartBuyPrice`, `window._chartBuyTime`
- OB_KEY: `pc_ob_v6_TGID` (tgId-specific localStorage key)
- Chart container IDs: `sd-price-chart` (signal), `detail-price-chart` (position), `bet-detail-chart` (bet)
- `_onPortfolioOnboardingCheck(serverOnboarded)` callback pattern (no setTimeout race)
- Gamma lookup via `clob_token_ids` param (not `conditionId` — returns wrong markets)
- Onboarding: `wallet-create.js` + `bot-v2.js` both set `onboarded: false` on creation

#### IDs & Credentials
- virixlabs tg_id: `7576704052`; alienjesus tg_id: `399089761`
- Master wallet: `0x6f314d7d2f50808cec1d26c1092e7729d9378d75` (Polygon)
- Bot: `@PolyClawsterBot`, token: `8721816606:AAHGpKrz2qNAoXwbguAQlEzYKj1TSkZdA4k`
- Supabase project: `hlcwzuggblsvcofwphza`
- Deploy: `git push app main` (remote `app` = GitHub → Vercel auto-deploy)

---
## Session update: 2026-03-07 — master wallet clarification

### Два кошелька — НЕ путать:
| Адрес | Роль |
|-------|------|
| `0xd58BE7E2Ac45ac822adFab5C1845e8016679a5eA` | **МАСТЕР-кошелёк** (polymarket-creds.json) — CLOB API ключ зарегистрирован, $18.55 USDC.e |
| `0x6f314d7d2f50808cec1d26c1092e7729d9378d75` | Личный кошелёк Ильи для alien account — НЕ мастер, unfunded |

### Stars → USDC архитектура — ГОТОВО
- `handleStarsPayment()` теперь отправляет реальный USDC.e с мастер-кошелька на кошелёк юзера
- Fallback: если кошелька нет → пишет в `stars_balance` до создания кошелька
- `api/wallet.js` теперь проверяет оба контракта: USDC.e + native USDC
- `stars_balance` обнулён для alienjesus (399089761) — был $1.014, конвертирован → on-chain USDC.e
- `edge/index.js` и `edge/cron.js` исправлены → используют `POLY_WALLET_ADDRESS` env или `0xd58BE7E2`
- `tma/api/server.js` исправлен → `0xd58BE7E2`

### Мастер-кошелёк состояние (2026-03-07)
- Balance: ~$18.55 USDC.e на Polygon
- CLOB API key: в polymarket-creds.json (НЕ коммитить!)
- POLY_PRIVATE_KEY: прописан в .env на PM2 сервере ✅

### TODO после исправлений
- Проверить что edge-runner видит позиции мастер-кошелька (теперь смотрит на 0xd58BE7E2)
- Пополнить мастер-кошелёк если баланс упадёт < $5

---
## Session итог: 2026-03-07 (вечер)

### UI фиксы TMA (все задеплоены на Vercel)

| Фикс | Описание |
|------|----------|
| Кнопка "Пополнить" на главной | Теперь сразу открывает depositSheet, не переходит на Wallet таб |
| После ставки → портфель | `goToPortfolioFresh()` — мгновенно показывает skeleton, потом загружает данные |
| YES/NO кнопки | Дизейблятся + показывают ⏳ пока ставка обрабатывается (и real, и demo) |
| 🔒 → 💰/⭐ иконка | Заменена на 💰 (или ⭐ если есть Stars баланс) — нет путаницы с замком |
| Stars в hero balance | `realFree = val + stars` — stars_balance включён в расчёт баланса портфеля |
| Stars retry refresh | После оплаты Stars: 3 попытки обновления (1с / 3с / 6с) |
| Wallet Live mode | `freeReal = freeUsdc + stars` — Stars учитывается в Live балансе |
| api/wallet.js | Проверяет оба USDC контракта: USDC.e (0x2791) + native (0x3c499) |

### Stars → USDC архитектура (задеплоено + активно)

- `handleStarsPayment()` в bot-v2.js переписан:
  1. Stars payment → `sendUsdcFromMaster(toAddress, usdValue)` 
  2. Мастер-кошелёк `0xd58BE7E2` отправляет реальный USDC.e на кошелёк юзера
  3. `deposit-watcher` автоматически засчитывает входящий USDC
  4. Fallback: если нет кошелька → пишет в `stars_balance`
  5. Fallback: если transfer fail → пишет в `stars_balance`
- Claw-1 (PM2 сервер): нашёл POLY_PRIVATE_KEY в polymarket-creds.json, прописал в .env, рестартовал бот
- Claw-1: отправил $1.014 USDC.e ретроактивно на кошелёк alienjesus, обнулил stars_balance

### Исправление адреса мастер-кошелька

| Файл | До | После |
|------|----|-------|
| tma/api/server.js | 0x6f314d7d (неверный) | 0xd58BE7E2 (мастер) |
| edge/index.js | хардкод 0x6f314d7d | env POLY_WALLET_ADDRESS или 0xd58BE7E2 |
| edge/cron.js | хардкод 0x6f314d7d | env POLY_WALLET_ADDRESS или 0xd58BE7E2 |

**Два кошелька — НЕ путать:**
- `0xd58BE7E2...` = мастер (CLOB ключ, ~$18.55 USDC.e, торгует)
- `0x6f314d7d...` = личный кошелёк Ильи для alien account (создан ранее, unfunded)

### Victory Posts в каналы (новая фича)

**Новые файлы:**
- `edge/modules/channels.js` — хранение каналов в `/workspace/edge/data/channels.json`

**Обновлённые файлы:**
- `edge/modules/notify.js` → `sendToAllChannels(msg, extra)` 
- `edge/modules/tracker.js` → `checkUserBets()` + `postVictoryToChannels(trade, user)` — вызывается при каждом winning trade
- `edge/index.js` → вызывает `checkUserBets()` в каждом цикле
- `tma/bot-v2.js` → авто-регистрация каналов через `my_chat_member`, команды `/channels`, `/addchannel`, `/removechannel`

**Логика:**
1. Бот добавлен в канал как admin → авто-регистрируется → уведомляет owner
2. Мастер-кошелёк trade закрылся в плюс → `postVictoryToChannels` → постит во все каналы
3. Юзерская ставка закрылась в плюс → показывает @username + кошелёк + результат
4. Формат поста: 🏆 Win, цифры P&L, кнопка "Открыть PolyClawster"

**Статус:** Claw-1 задеплоил (git pull + pm2 restart bot + edge)

### Маркетинг (обсуждение)
- Стратегия Моргенштерна применима: продавать результат, не продукт
- Главная идея: автоматические посты побед в каналы = органический контент
- Victory posts = первый шаг виральной петли

### Последние коммиты сессии
- `bca1682` — Пополнить→depositSheet, goToPortfolioFresh, YES/NO loading
- `ac14c79` — stars в hero balance + retry refresh
- `c13f6d5` — (remote)
- `afadbf7` — starsBalance в wallet Live mode + иконка 💰/⭐
- `32a0195` — Stars → real USDC transfer + wallet USDC.e fix
- `dc2b9f8` — master wallet адрес исправлен везде + memory
- `57c21a8` — victory posts в каналы + channel auto-registration
